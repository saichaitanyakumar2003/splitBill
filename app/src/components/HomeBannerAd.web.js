import React from 'react';

/**
 * Web stub: AdMob has no native component on web, so we render nothing.
 * This file is used when building for web so we never import react-native-google-mobile-ads.
 */
export default function HomeBannerAd() {
  return null;
}
