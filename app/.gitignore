node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Vercel
.vercel

# Firebase config files
# Note: google-services.json API keys are restricted to your app's package name
# and SHA-1 certificate, so they're safe to commit (Google's recommendation)
# google-services.json
GoogleService-Info.plist